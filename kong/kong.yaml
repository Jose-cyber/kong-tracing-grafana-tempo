_format_version: "2.1"

services:
  - name: httpbin
    url: http://httpbin:80
    routes:
      - name: httpbin-route
        paths:
          - /httpbin

plugins:
  - name: zipkin
    config:
      http_endpoint: http://tempo:9411/api/v2/spans
      sample_ratio: 1.0
